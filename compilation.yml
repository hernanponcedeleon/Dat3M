# $DAT3M_HOME and $DAT3M_OUTPUT can be used in paths. A standalone $DAT3M_COMPILER_OPTIONS entry
# in a command's options is replaced with the flags from the corresponding environment variable,
# or omitted when it is unset.
# Custom paths must be absolute and have the correct write permissions.
# Each command declares an input path and an output path. Their resolved values replace
# {cmd_input} and {cmd_output}, respectively, in that command's args.
# Use input: "{pipeline_input}" to read the program file given to Dartagnan
# (the CLI program path or the temporary source file created by the UI).
# {basename} is that file's name without its extension: shader.slang becomes shader.
# Every other command input, command output, and pipeline output is resolved below workdir
# unless it is an absolute path. For example, with workdir: "/tmp/dat3m", input: "foo.spv"
# means "/tmp/dat3m/foo.spv", while input: "/tmp/foo.spv" remains "/tmp/foo.spv".

workdir: "$DAT3M_OUTPUT"

x-common_commands:
  upgrade_cmd: &upgrade_cmd
    name: "Upgrade to Vulkan memory model"
    tool: "spirv-opt"
    input: "{basename}.spv"
    output: "{basename}-vulkan.spv"
    options: ["--upgrade-memory-model"]
    args: ["{cmd_input}", "-o", "{cmd_output}"]

  disassemble_cmd: &disassemble_cmd
    name: "Disassemble"
    tool: "spirv-dis"
    input: "{basename}-vulkan.spv"
    output: "{basename}.spvasm"
    options: []
    args: ["{cmd_input}", "-o", "{cmd_output}"]

  annotation_cmd: &annotation_cmd
    name: "Annotate inputs, outputs and program_spec"
    tool: "$DAT3M_HOME/scripts/copy_spv_header_annotations.bash"
    input: "{pipeline_input}"
    output: "{basename}.spvasm"
    options: []
    args: ["{cmd_input}", "{cmd_output}"]

pipelines:
  - pipeline: ".c"
    aliases: [".i"]
    output: "{basename}.ll"
    commands:
      - name: "Compile"
        tool: "clang"
        input: "{pipeline_input}"
        output: "{basename}.ll"
        options: ["-I", "$DAT3M_HOME/include", "-Xclang", "-disable-O0-optnone", "-S", "-emit-llvm", "-g", "-gcolumn-info", "$DAT3M_COMPILER_OPTIONS"]
        args: ["{cmd_input}", "-o", "{cmd_output}"]

  - pipeline: ".slang"
    output: "{basename}.spvasm"
    commands:
      - name: "Compile"
        tool: "slangc"
        input: "{pipeline_input}"
        output: "{basename}.spv"
        options: ["-target", "spirv", "-g", "$DAT3M_COMPILER_OPTIONS"]
        args: ["{cmd_input}", "-o", "{cmd_output}"]
      - *upgrade_cmd
      - *disassemble_cmd
      - *annotation_cmd

  - pipeline: ".cl"
    output: "{basename}.spvasm"
    commands:
      - name: "Compile"
        tool: "clspv"
        input: "{pipeline_input}"
        output: "{basename}.spv"
        options: ["--cl-std=CL2.0", "--inline-entry-points", "--spv-version=1.6", "-g", "$DAT3M_COMPILER_OPTIONS"]
        args: ["{cmd_input}", "-o", "{cmd_output}"]
      - *upgrade_cmd
      - *disassemble_cmd
      - *annotation_cmd

  - pipeline: ".comp"
    output: "{basename}.spvasm"
    commands:
      - name: "Compile"
        tool: "glslangValidator"
        input: "{pipeline_input}"
        output: "{basename}.spv"
        options: ["-V", "--target-env", "vulkan1.3", "-g", "$DAT3M_COMPILER_OPTIONS"]
        args: ["{cmd_input}", "-o", "{cmd_output}"]
      - *upgrade_cmd
      - *disassemble_cmd
      - *annotation_cmd

  - pipeline: ".hlsl"
    output: "{basename}.spvasm"
    commands:
      - name: "Compile"
        tool: "dxc"
        input: "{pipeline_input}"
        output: "{basename}.spv"
        options: ["-T", "cs_6_0", "-E", "main", "-spirv", "-fspv-target-env=vulkan1.1spirv1.4", "$DAT3M_COMPILER_OPTIONS"]
        args: ["{cmd_input}", "-Fo", "{cmd_output}"]
      - *upgrade_cmd
      - *disassemble_cmd
      - *annotation_cmd
