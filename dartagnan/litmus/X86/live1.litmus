X86 live1 (live1) 
"Test for liveness, does store buffers get flushed eventually?"
{x = 0; y = 0};

                P0      |       P1         ;
l1:  mov EAX,[x]  	|     mov [x], 1   ;
     cmp EAX,0 	        |                  ;
     je l1              | l2: mov EBX, [y] ;
                        |     cmp EBX, 0   ;
     mov [y],1          |     je l2        ;

forall (P0:EAX = 1 /\ P1:EBX = 1);

