X86 iwp2.2/amd2 (iwpTwoTwo) "Stores are not reordered with older loads"
{
 x = 0; y = 0;
}
  P0           |  P1           ;
  mov EAX, [x] |  mov EBX, [y] ;
  mov [y], 1   |  mov [x], 1   ;

exists (0:EAX = 1 /\ 1:EBX = 1);