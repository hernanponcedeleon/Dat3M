C a2_reorder+rel+Rna
{ [x] = 0; [y] = 0; [zero] = 0; }

P0 (volatile int* x, volatile int* y) {
  SCAS(x, zero, 1, memory_order_release, memory_order_relaxed);
  int t0 = *y;
}

P1 (volatile int* x, volatile int* y) {
  int r1 = atomic_load_explicit(x, memory_order_acquire);
  if (r1) {
    *y = 1;
  }
}
